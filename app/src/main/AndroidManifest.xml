<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.qimo">

    <uses-permission android:name="android.permission.VIBRATE"/><!--震动权限-->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Signreward.RewardActivity1"></activity>
        <activity android:name=".collectidioms.DetailsCtActivity" />
        <activity android:name=".collectidioms.ListCtActivity" />
        <activity android:name=".collectidioms.CollectActivity" />
        <activity android:name=".settingfragment.MusicActivity" />

        <service
            android:name=".settingfragment.MusicService"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".Signreward.RewardActivity" />
        <activity android:name=".gamefragment.Game1Activity" />
        <activity android:name=".checkpoint.CheckpointActivity" />
        <activity android:name=".GameActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>